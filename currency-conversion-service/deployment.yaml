apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  labels:
    app: currency-conversion-service-service
  name: currency-conversion-service-service
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: currency-conversion-service-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: currency-conversion-service-service
    spec:
      containers:
        - image: 22071996/mmv2-currency-conversion-service-service:0.0.1-SNAPSHOT
          imagePullPolicy: IfNotPresent
          name: mmv2-currency-conversion-service-service
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    cloud.google.com/neg: '{"ingress":true}'
  finalizers:
    - service.kubernetes.io/load-balancer-cleanup
  labels:
    app: currency-conversion-service-service
  name: currency-conversion-service-service
  namespace: default
spec:
  ports:
    - port: 8888
      protocol: TCP
      targetPort: 8888
  selector:
    app: currency-conversion-service-service
  sessionAffinity: None
  type: LoadBalancer